using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugCanvasPopulator : MonoBehaviour
{
    //Dictionary of names and values (values can be anything that's ToString()able, ie., IFormattable)
    //Add to dictionary via action
    //When dictionary is added to, add element to debug canvas
    //debug canvas uses grid layout group or something to organize elements
}
