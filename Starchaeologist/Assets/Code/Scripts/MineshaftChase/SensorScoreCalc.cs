using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SensorScoreCalc : MonoBehaviour
{
    //SensorData includes 3 floats representing the 3 axes of rotation of each sensor
    //private List<SensorData>() data;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //----------------------------------------------------------------------------------------------------------------------------------
    /*
     * Sensor System Pseudocode
     * 
     *score = starting value - deviation from goal rotation of average of rotations of sensors
     */
}
